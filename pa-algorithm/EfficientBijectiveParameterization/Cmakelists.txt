#Efficient Bijective Parameterization by Jianping Su
project(EfficientBijectiveParameterization)

if (${LINSYSSOLVER} STREQUAL "Eigen")
    include_directories(SYSTEM ${EIGEN_PATH})
    add_compile_definitions(-DUSE_EIGEN)
elseif(${LINSYSSOLVER} STREQUAL "MKL")
    add_compile_definitions(-DUSE_MKL)
endif()

include_directories(${EIGEN_PATH})
include_directories(${CMAKE_SOURCE_DIR}/pa-interface)
include_directories(SYSTEM ${TRIANGLE_PATH})
include_directories(SYSTEM ${CGAL_PATH}/include)
include_directories(${BOOST_PATH}/include/boost-1_87)

file (GLOB all_files
    BiljectivePara.h
    BiljectivePara.cpp
    Common.h
    Common.cpp
    Parafun.h
    Parafun.cpp
    ShellData.h
    ShellData.cpp
)

add_library(EfficientBijectiveParameterization ${all_files})